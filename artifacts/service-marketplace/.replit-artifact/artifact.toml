kind = "web"
previewPath = "/"
title = "Service Marketplace"
description = "An Arabic-first RTL service marketplace MVP connecting beneficiaries with service providers through a clear onboarding flow and role-based home experiences."
version = "1.0.0"
id = "artifacts/service-marketplace"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25067

[services.development]
run = "pnpm --filter @workspace/service-marketplace run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/service-marketplace", "run", "build" ]
serve = "static"
publicDir = "artifacts/service-marketplace/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25067"
BASE_PATH = "/"
